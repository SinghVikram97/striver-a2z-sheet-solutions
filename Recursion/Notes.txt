1. (a*b)%M = ((a%M)*(b%M))%M

2. Binary Exponentiation: 
pow(a,n) =
  if n%2==0 , pow(a*a,n/2)
  if n%2==1,  pow(a,n-1)

3. Combination Sum and Subset Sum with duplicates

4. N queen - Pigenhole principle